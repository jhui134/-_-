import {StyleSheet} from 'react-native';
import {Colors} from '../../shared/colors';

export const overlaySpinnerStyles = StyleSheet.create({
  wrap: {
    backgroundColor: Colors.white,
    flex: 1,
    justifyContent: 'center',
  },
});
