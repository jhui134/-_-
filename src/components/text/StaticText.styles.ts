import {StyleSheet} from 'react-native';
import {Colors} from '../../shared/colors';

export const StaticTextStyles = StyleSheet.create({
  text: {
    fontSize: 14,
    color: Colors.black,
  },
});
