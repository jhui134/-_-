import {StyleSheet} from 'react-native';
import {Colors} from '../../shared/colors';

export const photoVerticalScreenStyles = StyleSheet.create({
  wrap: {
    flex: 1,
    backgroundColor: Colors.white,
  },
});
