import {StyleSheet} from 'react-native';
import {Colors} from '../../shared/colors';

export const photoHorizontalScreenStyles = StyleSheet.create({
  wrap: {
    flex: 1,
    backgroundColor: Colors.white,
  },
});
