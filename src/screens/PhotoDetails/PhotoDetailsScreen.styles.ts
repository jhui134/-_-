import {StyleSheet} from 'react-native';
import {Colors} from '../../shared/colors';

export const photoDetailsScreenStyles = StyleSheet.create({
  wrap: {
    flex: 1,
    backgroundColor: Colors.white,
  },
  lottie: {
    flex: 1,
  },
});
